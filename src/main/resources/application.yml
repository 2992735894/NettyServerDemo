server:
  port: 8086

rocketmq:
  name-server: 127.0.0.1:9876
  producer:
    group: test-group
    send-message-timeout: 3000
    retry-times: 3
  consumer:
    group: test

spring:
  data:
    mongodb:
      uri: mongodb://ljdll:ljdll@localhost:27017/ljdll?authSource=admin
  datasource:
    druid:
      validation-query: SELECT 1


mybatis-flex:
  datasource:
    dm8:
      driverClassName: dm.jdbc.driver.DmDriver
      url: jdbc:dm://localhost:5236/LJDLL?characterSet=UTF-8&autoCommit=false
      username: LJDLL
      password: cangshu924
    mysql:
      driverClassName: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/mysql_test?characterSet=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
      username: test
      password: cangshu924

